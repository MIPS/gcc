# We default to building libraries optimised for size.
# -mno-gpopt has two purposes: it allows libraries to be used in situations
# where $gp != our _gp, and it allows them to be built with -G8 while
# retaining link compatibility with -G0 and -G4.
CFLAGS_FOR_TARGET += -Os -mno-gpopt -ffunction-sections -fdata-sections
CXXFLAGS_FOR_TARGET += -Os -mno-gpopt -ffunction-sections -fdata-sections
