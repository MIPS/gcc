# Don't let CTOR_LIST end up in sdata section.
CRTSTUFF_T_CFLAGS = -G 0

# Don't add unwind info to .eh_frame for crtend after the terminating zero.
CRTSTUFF_T_CFLAGS += -fno-asynchronous-unwind-tables